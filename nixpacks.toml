[phases.setup]
nixPkgs = ["nodejs_20", "pnpm"]

[phases.install]
cmds = [
  "pnpm install --frozen-lockfile"
]

[phases.build]
cmds = [
  "pnpm --filter web build"
]

[start]
cmd = "cd apps/web && pnpm start"

[variables]
NODE_ENV = "production"
PORT = "3000"